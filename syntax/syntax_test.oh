_unused_variable, _unused_this_is, _unused
_unused_function_, _un_used_function_, _unused_,

fn_(~t:): never_
fn_(str;): ~t_

infer[~]: whatever_()

# this is a one-letter function, named `_`:
_(x: int): null_

$lambda_var $lambda_fn_

"string ${interpolation works_() {} [] too?} $[1.23, a, b_(), [], {}] $(name: value_(), [], {})"

used{asdf} used[asdf] used(asdf)
used_{asdf} used_[asdf] used_(asdf)
_unused{asdf} _unused[asdf] _unused(asdf)
_unused_{asdf} _unused_[asdf] _unused_(asdf)
f*234_(asdf)
743_567+1234_5e7
-1-123_456.789_e+10+7.123_456e-3

#[#
these lines should be commented out
this line too.
```
3 + 4 # inside here we can have nested comments
"ok, " + #( midline comment )# print_("this is good")
#{#
probably overkill to have nested multiline comments `with nested ticks_()`
#}#
```
#]#

my_function_(x: int_): int_
    x + 1

#[#
this line should be commented out
```
# bad namespace examples
    ASDF ASDF_A B_ASDF
# TODO: fix these
# bad namespaces but hard to detect with regex:
    ASDF_ _ASDF
# ok but weird namespaces
    ASDFi jASDF ASDFfn_ funcASDF_ qwASDFerty
# idomatic namespaces
    ASDF_i j_ASDF ASDF_fn_ func_ASDF_
    this_ASDF_variable some_ASDF_function_
    ASDF_ASDF_asdf asdf_ASDF_ASDF as_ASDF_ASDF_df
    fun_OK_DOKEY_ f_OK_DOKEY_un_ OK_DOKEY_fun_
# with unused.
     _ASDF_unused
```
#]#

6 + 1.2345

#{#
this line should be commented out
#}#

result_(u64): hm_[ok_: u64_, er_: u64_]
    if u64 % 2 == 0
        ok: u64 // 2
        ok
    else
        er: u64
        er

[import_type_]: \/this/is/a/path
[another_import_(arg1:, arg2;): ret3_, other_type_]: \/../another/path
renamed_: \\library/path whatever_type_
